#pragma once
#include <unordered_map>
#include <string>

namespace Environment
{
    using std::unordered_map;
    using std::string;

    extern unordered_map<string, double> Env;
    
} // Environment
